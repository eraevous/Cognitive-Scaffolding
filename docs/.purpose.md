# @codex-role: architect
# @codex-objective: generate or upgrade `.purpose.md` with:
# - output schema
# - coordination logic
# - integration points
# - ecosystem anchoring
# Follow AGENTS.md G-10 and Section 9 enrichment instructions.

## @ai-intent
- Capture design proposals for "Intentional Accretion" within `docs/intentional_accretion.md` so Kairos can adopt a stable spine with extensible sediment.
- Provide future contributors with meta-rules, reconciliation hooks, and precedent logging patterns aligned to the CHARTER.

## io.schema
| direction | name | type | description | constraints |
| --- | --- | --- | --- | --- |
| input | design_drivers | List[str] | Paradigm prompts and charter principles that inform accretive design. | Must reference CHARTER invariants (config, schema validation, deterministic IDs). |
| output | accretion_notes | Markdown sections | Structured sections detailing candidate cores, edges, and next steps. | Sections must specify spine invariants, edge behaviors, and governance hooks. |

## coordination mechanics
- `architect` agent authors and curates the Markdown; `memory_architect` reconciles future edits with ledger/precedent plans.
- Shared inputs: CHARTER rules, AGENTS Run/Drift cadence, existing pipeline domains (ingestion, embedding, retrieval, synthesis).
- Message shape: `{section_id, spine, edges, governance_hooks, actions}` for future automation to parse.

## integration points
- Upstream: roadmap conversations and `.intent.md` trails that supply new design drivers.
- Downstream: pipeline refactors in `src/core/*`, CLI planners in `src/cli`, and governance utilities (ledger/reconciler) that will materialize the proposed cores.
- External: BudgetTracker or validation hooks may consult the recommended precedent store for approvals before expensive actions.

## @ai-role
- Primary: architect
- Supporting: memory_architect, meta_guard (validates schema alignment when proposals become code)

## risks
- Schema drift if downstream implementations diverge from documented spines; mitigate via reconciliation loop prototypes.
- Over-specification could freeze accretion; keep the spine minimal and formalize only after stability.
