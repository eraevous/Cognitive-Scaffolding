prompt_content = f"""
Read the following document and extract structured metadata.

Return ONLY a valid JSON object with the following keys:
- summary
- topics (list)
- category (string)
- tags (list)
- themes (a concise central idea or throughline)
- priority (1–5)
- tone
- depth (personal / technical / philosophical)
- stage

themes: a list of one or more short phrases that captures the main idea, underlying thread, or purpose that ties the document together (e.g., "Reclaiming agency through narrative", "Ethical tradeoffs in automation", etc.)

Document:
\"\"\"
{content}
\"\"\"
    """

    body = {
        "messages": [
            {
                "role": "user",
                "content": prompt_content
            }
        ],
        "max_tokens": 500,
        "temperature": 0.5,
        "anthropic_version": "bedrock-2023-05-31"
    }